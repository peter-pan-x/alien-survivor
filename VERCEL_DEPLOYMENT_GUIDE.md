# Vercel部署教学指南

## 🚀 项目部署到Vercel完整指南

### 📋 前置条件
- ✅ GitHub仓库：`https://github.com/peter-pan-x/alien-survivor`
- ✅ Vercel账号已登录
- ✅ 项目已完成本地构建测试

### 🎯 方案一：GitHub集成部署（推荐）

#### 步骤1：进入Vercel导入页面
1. 访问：`https://vercel.com/new`
2. 确保你已经登录Vercel账号
3. 选择 **"Import Git Repository"** 选项

#### 步骤2：连接GitHub
1. 点击 **"Continue with GitHub"**
2. 授权Vercel访问你的GitHub账号
3. 在仓库列表中找到 `alien-survivor` 项目
4. 点击 **"Import"** 按钮导入项目

#### 步骤3：项目配置（已自动配置）
项目已包含完整的配置文件：

**package.json** ✅
- 自动包含所有依赖和构建脚本
- Vercel会自动识别React+Vite项目

**vercel.json** ✅
- 预配置的构建设置
- 输出目录：`dist/public`
- SPA路由重写规则

**自动识别设置：**
- **Framework Preset**: 自动识别为 **"Vite"**
- **Build Command**: `npm run build`
- **Output Directory**: `dist/public`
- **Root Directory**: `./`

#### 步骤4：环境变量（如果需要）
检查项目是否需要环境变量，对于这个React游戏项目，通常不需要特殊环境变量。

#### 步骤5：部署
1. 点击 **"Deploy"** 按钮
2. 等待构建完成（通常2-3分钟）
3. 获得部署后的URL地址

---

### 🛠️ 方案二：Vercel CLI部署

#### 步骤1：安装Vercel CLI
```bash
# 使用npm全局安装
npm install -g vercel

# 或者使用npx（推荐）
npx vercel@latest
```

#### 步骤2：项目配置文件
确保项目根目录有正确的配置：

**package.json 确认：**
```json
{
  "name": "alien-survivor",
  "version": "2.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  }
}
```

**vercel.json（可选，用于自定义配置）：**
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install"
}
```

#### 步骤3：登录和部署
```bash
# 在项目根目录执行
cd C:\Users\PETER\Desktop\super-warrior-game\alien-survivor-main

# 登录Vercel
npx vercel login

# 部署项目
npx vercel --prod
```

#### 步骤4：配置交互式界面
CLI会询问以下问题：
- **Set up and deploy?** → `Y`
- **Which scope?** → 选择你的Vercel账号
- **Link to existing project?** → `N`（首次部署）
- **Project name?** → `alien-survivor`
- **In which directory is your code located?** → `./`
- **Want to override the settings?** → `N`（使用默认Vite配置）

---

### ⚙️ 项目特定配置

#### 1. vite.config.ts 确认
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/', // Vercel部署保持根路径
  build: {
    outDir: 'dist',
    sourcemap: false, // 生产环境关闭sourcemap
    minify: 'terser',
    rollupOptions: {
      output: {
        manualChunks: undefined // 不分割代码，适合小项目
      }
    }
  },
  server: {
    host: true
  }
})
```

#### 2. index.html 路径配置
确保所有资源路径使用相对路径：
```html
<link rel="icon" type="image/svg+xml" href="/vite.svg" />
```

#### 3. 路由处理（SPA）
Vercel会自动处理React SPA的路由，无需额外配置。

---

### 🎮 游戏项目特殊注意事项

#### 1. 静态资源处理
- 游戏中的图片、音频文件放在 `public/` 目录
- 确保 `public/` 目录中的所有文件都会被正确复制到 `dist` 目录

#### 2. Canvas性能优化
- 确保游戏在生产模式下正常运行
- 检查移动端触摸控制是否正常工作

#### 3. HTTPS和安全性
- Vercel自动提供HTTPS
- 确保游戏资源加载使用HTTPS协议

---

### 🐛 常见问题和解决方案

#### 问题1：构建失败
**解决方案：**
```bash
# 本地测试构建
npm run build

# 检查package.json scripts
npm run preview
```

#### 问题2：部署后白屏
**可能原因：**
- 路径配置错误（检查 `base` 配置）
- JavaScript错误（检查浏览器控制台）
- 资源加载失败（检查静态资源路径）

**解决方案：**
```typescript
// vite.config.ts
export default defineConfig({
  base: './', // 使用相对路径
})
```

#### 问题3：游戏无法加载资源
**检查清单：**
- [ ] 所有静态文件都在 `public/` 目录
- [ ] 路径使用正确的相对引用
- [ ] 没有使用绝对本地路径

#### 问题4：移动端控制失效
**检查事项：**
- 确保触摸事件监听正确绑定
- 检查虚拟摇杆是否正确渲染
- 验证Canvas尺寸适配

---

### 📊 部署后验证

#### 1. 功能检查清单
- [ ] 游戏能够正常加载和启动
- [ ] 角色移动和射击功能正常
- [ ] 技能升级系统工作正常
- [ ] Boss战斗功能正常
- [ ] 音效和背景音乐播放正常
- [ ] 移动端触摸控制正常
- [ ] 响应式设计在不同屏幕尺寸下正常

#### 2. 性能检查
- [ ] 首次加载时间 < 5秒
- [ ] 游戏运行流畅（60 FPS）
- [ ] 内存使用合理
- [ ] 没有控制台错误

#### 3. URL获取
部署成功后，你会获得：
- **主要URL**: `https://alien-survivor.vercel.app`
- **自定义域名**: 可在Vercel控制台设置

---

### 🔄 更新部署

#### 方法1：自动部署（GitHub集成）
- 推送代码到main分支 → 自动触发重新部署
- Vercel会自动构建和部署最新版本

#### 方法2：手动更新（CLI）
```bash
# 本地提交后推送
git add .
git commit -m "Update game features"
git push origin main

# 手动触发重新部署
npx vercel --prod
```

---

### 📈 监控和分析

#### Vercel Analytics
1. 在Vercel控制台启用Analytics
2. 监控页面加载速度
3. 查看用户访问统计
4. 分析性能瓶颈

#### 错误监控
- 定期检查Vercel Function Logs
- 监控构建失败原因
- 跟踪用户报告的运行时错误

---

### 🎯 部署成功后的URL

部署完成后，你的游戏将在以下地址可用：
- **主要URL**: `https://alien-survivor.vercel.app`
- **GitHub Pages**: `https://peter-pan-x.github.io/alien-survivor/`（已有）

---

## 💡 总结

1. **推荐方案**: 使用GitHub集成部署，设置一次后自动更新
2. **关键配置**: 确保vite.config.ts和package.json正确配置
3. **验证重要**: 部署后要全面测试所有游戏功能
4. **持续更新**: 利用GitHub的自动部署功能保持版本同步

按照这个指南操作，你应该能够成功将异星幸存者游戏部署到Vercel平台！🚀